import styled from 'styled-components';

export const ImagePickerStyles = {
  Wrapper: styled.div`
    display: flex;
    align-items: center;

    input {
      display: none;
    }
  `,

  Button: styled.div`
    display: flex;
    cursor: pointer;
  `,
};
