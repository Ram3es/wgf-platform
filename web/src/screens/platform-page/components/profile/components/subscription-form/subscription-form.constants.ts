export const subscriptionLabels: Record<string, string> = {
  date: 'Subscription Date',
  duration: 'Subscription Duration',
  expiration: 'Subscription Expiration',
};

export const SubscriptionItems = Object.keys(subscriptionLabels);
